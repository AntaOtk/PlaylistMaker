<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.example.playlistmaker.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlaylistMaker"
        tools:targetApi="31">
        <activity
            android:name=".main.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".settings.ui.activiy.SettingsActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".libraby.ui.activity.LibraryActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".search.ui.activity.SearchActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".player.ui.activity.AudioPlayer"
            android:screenOrientation="portrait"
            android:exported="false">
        </activity>
    </application>
</manifest>
